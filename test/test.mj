program MJProgram

const int a = 3, b = 5;

char x, y[];
//set s1;
//int s3;
//char s2;
set s1, s2, s3;
int niz[];
int index;
int nijeNiz;
char pogresanIndex;
	
{

	int vratiBroj() {
       return 42; // Ispravno: metoda vraca int, izraz je int.
        // return 'a';  //Greska: Tip povratne vrednosti ne odgovara povratnom tipu metode 'vratiBroj'.
    }

    char vratiSlovo() char c; {
        c = 'A';
        return c; // Ispravno: metoda vraca char, izraz je char.
        //return; // Greska Metoda 'vratiSlovo' nije void i mora vratiti vrednost.
    }

    void neVracaNista() {
        print(1);
        
        //return; // Ispravno: void metoda koristi return bez izraza.
        //return 100; // Greska: Metoda 'neVracaNista' je void i ne sme vracati vrednost.
    }
    
    void implicitniReturn() {
        // Ispravno: void metoda ne mora imati return, implicitno se vraca na kraju.
    }
	
	
	void main() int s, x, s1[]; char a; {
	
		nijeNiz = 10;
        //nijeNiz[5] = 100; //greska
        
        pogresanIndex = 'a';
        //niz[pogresanIndex] = 100; //greska
        //nijeNiz[pogresanIndex] = 100; //gr
	
		index = 5;
        
        niz[0] = 10;          // Pristup sa literalom kao indeksom
        niz[index] = 20;      // Pristup sa promenljivom kao indeksom
        niz[index - 3] = 30;  // Pristup sa izrazom kao indeksom
        
        print(niz[2]);
	
		//print(vratiBroj());
        //print(vratiSlovo());
        //neVracaNista();
		//s1 = new int[10];
		//s2 = new set[8];
		//print(1, 3); //radi
		//print(s1, 3); //radi
		//print(false, 3); //radi
		//print(h[3]); //radi
		//print(h); //greska ne moze niz
		//x=3; //radi. dekl imena u unutrasnjem opsegu sakriva dekl istog imena u spoljasnjem opsegu
		//s1 = s2 union s3;
		//read(y[2]);
		//read(b); //ne moze read nad Con samo Var i Elem
		//greske:
		//s= 3+a-2; 
		//print(h['a']);
		
		// print(y[7]);  //radi
		// s= 3 + h[2]*2;  //radi
		// b=3;
		//s=h;
		//s = 4;  //radi
		//h = new int[a];
		//h = new int[3];  //radi
		
		//h=s;
		//h[2]=a;
		//h[2]=s;  //radi
		
		//h=null; //radi
		//h[2]=h[3];  //radi
		
		//a++;
		//h++;
		//h[2]--; //radi
		//b--;
	}
	
	int f( int x, char b, int f, bool k) int s; {
	
		//main();
		//mem(); //greska 
		//x = vratiBroj();
		//k= vratiBroj(); //greska
		return 0;
		
	}
	
	int fuc(int x){ return x;}
	int fgh(int x, bool b, int a){return fuc(x)+1;}
	
	int dg3(){ 
	   fgh( fgh(1, true, 3), false, fgh(1, true, 5) ); 
	   return 1; }
	
	//int mem(){}
	
}